apiVersion: v1
kind: Service
metadata:
  name: {{ include "firebird_exporter.fullname" . }}
  labels:
    {{- include "firebird_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firebird_exporter.selectorLabels" . | nindent 4 }}
